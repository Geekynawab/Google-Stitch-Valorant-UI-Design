{%- comment -%} Derive patch number {%- endcomment -%}
{%- assign patch_num = nil -%}
{%- for tag in article.tags -%}
  {%- if tag contains 'patch:' -%}{%- assign patch_num = tag | remove: 'patch:' -%}{%- endif -%}
{%- endfor -%}
{%- assign patch_num = patch_num | default: article.published_at | date: '%y.%m' -%}

<div class="container">{% render 'breadcrumb' %}</div>

<article class="article-page">

  <div class="article-patch-header">
    <div class="container">
      <span class="article-patch-eyebrow">// PATCH NOTES</span>
      <span class="article-patch-number">{{ patch_num }}</span>
    </div>
  </div>

  <div class="container">
    <div class="article-layout">

      {%- if article.image -%}
        <div class="article-hero">
          <img src="{{ article.image | image_url: width: 1200 }}"
               alt="{{ article.image.alt | escape | default: article.title }}"
               loading="eager">
        </div>
      {%- endif -%}

      <h1 class="article-title">{{ article.title }}</h1>

      <div class="article-meta-bar">
        <span class="article-meta__date">{{ article.published_at | date: '%B %d, %Y' }}</span>
        {%- if article.author != blank -%}
          <span class="article-meta__sep">//</span>
          <span class="article-meta__author">{{ article.author | upcase }}</span>
        {%- endif -%}
        {%- if article.tags.size > 0 -%}
          {%- for tag in article.tags -%}
            {%- unless tag contains 'patch:' -%}
              <a href="{{ blog.url }}/tagged/{{ tag | handle }}" class="article-tag">{{ tag }}</a>
            {%- endunless -%}
          {%- endfor -%}
        {%- endif -%}
      </div>

      <div class="article-body rte">
        {{ article.content }}
      </div>

      <footer class="article-footer">
        <div class="article-footer__line"></div>
        <a href="{{ blog.url }}" class="article-back">
          <span class="material-symbols-outlined" style="font-size:15px;vertical-align:middle;">arrow_back</span>
          Back to {{ blog.title }}
        </a>
      </footer>

    </div>
  </div>

</article>

{% schema %}
{ "name": "Article Page" }
{% endschema %}
